import { each } from "lodash";
import { inputDatas } from "../model/user-input-data";
import { bubbleButton, mergeButton, sortingBoard, wholeNumberInput } from "../view/add-event-listeners";

export default function handleClickHelp() {
}
